{
  "name": "@f/color-names",
  "description": "Map of color names to RGBA values",
  "repository": "git://github.com/micro-js/color-names.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
